import streamlit as st
from dotenv import load_dotenv

# 環境変数の読み込み
load_dotenv()

# ページ設定
st.set_page_config(
    page_title="医薬品情報検索・比較分析システム",
    page_icon="💊",
    layout="wide",
    initial_sidebar_state="expanded"
)

# タイトル
st.title("💊 医薬品情報検索・比較分析システム")
st.markdown("---")

# サイドバー
with st.sidebar:
    st.header("設定")
    
    # 自社製品選択
    st.subheader("自社製品")
    selected_product = st.selectbox(
        "選択してください",
        ["血漿分画製剤A", "血漿分画製剤B", "IBD製剤", "抗うつ薬"]
    )
    
    # 比較対象選択
    st.subheader("比較対象")
    comparison_type = st.radio(
        "比較タイプ",
        ["他社製品と比較", "自社製品間比較"]
    )
    
    if comparison_type == "自社製品間比較":
        st.info("⚠️ 自社製品間の比較です")

# メインタブ
tab1, tab2, tab3, tab4 = st.tabs([
    "📊 総合比較ダッシュボード",
    "💰 コスト比較",
    "💊 製品別データ",
    "📈 市場・シェア動向"
])

with tab1:
    st.subheader("総合比較ダッシュボード")
    st.info("自社製品: **{}** | 比較タイプ: **{}**".format(selected_product, comparison_type))
    st.write("ここに総合比較のグラフ・表を表示します。")

with tab2:
    st.subheader("コスト比較")
    st.write("ここにコスト比較データを表示します。")

with tab3:
    st.subheader("製品別データ")
    st.write("ここに製品別の詳細データを表示します。")

with tab4:
    st.subheader("市場・シェア動向")
    st.write("ここに市場・シェアの動向を表示します。")